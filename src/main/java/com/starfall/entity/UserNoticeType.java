package com.starfall.entity;


public enum UserNoticeType {
    topic("topic"),
    live("live"),
    friend("friend"),
    msg("msg"),
    all("all"),

    tmp("tmp");

    UserNoticeType(String name) {
    }
}
